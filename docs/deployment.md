## Deployment

Generate Docker files for both backend and frontend and execute them using docker compose.
Generate the GitHub Action CI workflow to push the container images into a container registry.
